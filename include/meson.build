public_include_dir = include_directories('.')

# `install_subdir` is chosen over `install_headers` in order to preserve
# header directory structure
install_subdir('beryl', install_dir: get_option('includedir'))
